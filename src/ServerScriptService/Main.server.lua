require(script.SetupRemotes)
local DataService = require(script.DataService)
local WeaponService = require(script.WeaponService)
local CombatService = require(script.CombatService)
local KillEffectService = require(script.KillEffectService)
local AdminCommands = require(script.AdminCommands)

-- Services are required to initialize connections; nothing else needed here.
